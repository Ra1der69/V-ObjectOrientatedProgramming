#pragma once

// user contains, username, fullname, dob, password, 
	// can also incude adress for home deleivy for books, warnings for 
	// missing deadlines as well as to send letters over to remind users they are renting a book
	// user will have a set amount of books they can have at one time which is three

string USERNAME;
	int DOB;
	string PASSWORD;
	int bookLimit; // BookLimit = 3;

class User
{
	
	

	User(USERNAME, PASSWORD, DOB, BookLimit);
	
	
};

